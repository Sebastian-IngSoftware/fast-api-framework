services:
  app:
    build: .
    container_name: fast-api-framework
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: unless-stopped
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
